export const metadata = {
    title:"Login | Jobsy",
    description: "Login to your job seeker account and start applying!"
}

export default function Layout({children}){
    return(
        <div>{children}</div>
    )
}