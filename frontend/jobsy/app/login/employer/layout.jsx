export const metadata = {
    title:"Login | Jobsy",
    description: "Login to your employer account and start applying!"
}

export default function Layout({children}){
    return(
        <div>{children}</div>
    )
}