export const metadata = {
    title:"Create your employer account | jobsy",
    description:"Join Jobsy to hire the best candidates for your company."
}
export default function Layout({children}){
    return (
        <div>{children}</div>
    )
}